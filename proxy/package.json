{
  "type": "module",
  "scripts": {
    "dev": "node --watch reverse-proxy-dev.ts",
    "test": "vitest run",
    "types": "tsc --noEmit"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.2"
  }
}
