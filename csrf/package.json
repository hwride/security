{
  "type": "module",
  "scripts": {
    "dev-app": "node --watch server-app.ts",
    "dev-attacker": "node --watch server-attacker.ts",
    "proxy": "node --watch proxy.ts",
    "types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.9.3"
  }
}
